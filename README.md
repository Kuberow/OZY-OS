# OZY-OS
Most complicated cc:tweaked os ever. And it is for no reason.
STILL WORK IN PROGRESS


# Context
The OS runs in a Virtual CPU made in lua.
The Kernel is made entirely in C, making it complex to run the C code which is why it has a Virual CPU to run 16-bit binary from the C code
